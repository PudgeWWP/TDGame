# 网页试玩与更新

## 当前方式

网页版使用同一套 Godot 场景和脚本，不是另写一份游戏。导出预设为 `Web`，关闭线程和 PWA，使用兼容渲染器。

- 设计分辨率 720×1280，浏览器窗口内等比显示。
- 存档属于各自浏览器；清除网站数据会清除成长，GitHub 不同步玩家存档。
- 当前手机真机兼容性尚需试玩确认；推荐最新版 Chrome、Edge、Safari。
- 该版本是浏览器游戏，还不是微信小游戏体验版。

## 固定网站

- Sites 项目：`appgprj_6aa13e5b41008191875f4f6c2e18f36b`
- 已发布固定地址：https://mozhen-sanguo-tdgame.wpw2519.chatgpt.site
- 网站实际发布状态应以 Sites 返回结果为准，不可仅凭上述地址认定已上线。

## 给后续 Codex 的更新说明

1. 拉取 GitHub 最新内容，读取 AGENTS.md 并保留用户修改。
2. 安装与引擎版本相同的官方网页导出模板。
3. 使用 `Web` 预设导出到 `builds/web/index.html`。必须包含 `data/*.json`。
4. 用 `tools/prepare_web.cjs` 处理导出目录，第二个参数是网站的 `dist` 目录。它压缩 WASM/PCK、生成内容指纹文件名并验证压缩完整性。浏览器通过 DecompressionStream 解压，因此不依赖服务器自定义 Content-Encoding。
5. 使用现有 Sites 项目更新版本，不新建网站。源仓库是 Godot 项目的 GitHub；Sites 仓库是发布产物，两者用途不同。
6. 保留固定 URL，确认发布成功。仅上传 GitHub 目前不会自动更新网站；自动发布尚未配置。
7. 不把临时凭据、导出模板、builds 或玩家存档提交进 GitHub。

## 首版验证范围

记录实际导出、资源引用、压缩完整性和部署结果；未做手机真机测试前不要声称已验证。


2026-09-09：Sites 第一版已成功公开发布。战斗模拟、导出、脚本语法、资源引用、HTTP 加载与压缩完整性通过；未进行手机真机测试。
