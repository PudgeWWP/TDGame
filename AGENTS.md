# TDGame 项目规范与交接说明

本文件是本项目的长期说明。无论在公司电脑、家中电脑，还是开启新的 Codex 对话，都应先读取本文件，再继续开发。

## 1. 项目概况

- 项目名称：TDGame（暂定名）
- 项目类型：微信小游戏
- 游戏方向：三国题材、汉字变形美术、兵营自动出兵塔防。临时演示名“墨阵三国”。
- 开发引擎：Godot 4.7.1 stable
- 脚本语言：GDScript
- 主要操作方式：手机触摸；同时保留鼠标操作，方便电脑测试
- 目标画面：竖屏
- 设计分辨率：720 × 1280
- 电脑测试窗口：450 × 800
- 渲染器：Compatibility（兼容）
- 主场景：`res://main.tscn`

## 2. 当前进度

- 已创建 Godot 项目。
- 已创建并保存主场景 `main.tscn`。
- 已将主场景设为项目启动场景。
- 已设置竖屏、设计分辨率和电脑测试窗口尺寸。
- 已完成第一版可运行战斗演示。操作说明、数值默认值及验证方法见 `PLAYTEST.md`。
- 已完成第二轮基础内容扩展：采集兵实体往返搬运、下一波递减倒计时，以及基地/采集/刀营/弓营的 13 项独立强化入口。
- 已完成局外行营首页：玩家头像与个人信息、府库玉石、开始守城，以及商肆/战役/军略/校场四个入口。军略对应天赋研习，校场对应永久属性养成；具体子页面后续制作。
- 已完成主将档案页：称号、待接入账号系统的 UID 状态、音乐/音效/伤害数字开关、挑战统计，以及 BUG/建议反馈入口。三个开关默认开启并随存档保存。
- `battle_model.gd` 管理战斗状态，`battle_view.gd` 管理绘图、输入和存档；数值见 `data/balance.json`。
- 已验证战斗规则、六波完整模拟、按钮点击、结算、永久升级与存档重载。
- 未接入微信小游戏导出，未制作音效。美术为独立绘制的文字/矢量演示资源，后续用户提供正式素材替换。
- 下一步：用户试玩后按模块反馈修改，确认功能后再提供正式美术需求表。

### 用户已确认的战斗规则

- 竖屏，上方左/右两个采集点分别生产局内/局外资源；固定基地位于中上方，怪物从下半部分向上接近。
- 基地固定不动，不设置基地移动速度强化。
- 回合条为下一波到来时间，左侧为当前波次/总波次，与击杀数无关。
- 删除独立的当前武器展示。武器系统改为兵营自动出兵，士兵自动寻敌作战。
- 怪物遇到我方士兵停下持续攻击；没有附近士兵时接近基地，进入攻击距离后持续攻击基地，直到被消灭。
- 双资源独立显示和采集：局内用于本局强化；局外用于永久强化。战败后已采集局外资源全部带走。
- 随机间隔额外掉落局内资源，玩家点击拾取，超时未拾取自动消失。
- 两处资源均由采集兵在资源点与基地之间往返搬运，采集完成不立即入账，送回基地后才增加资源。
- 波次进度条显示下一波剩余时间，采用递减的倒计时；最后 5 秒转为红色提示。
- 下一波倒计时只在本波敌人全部清除后开始；交战期间进度冻结。敌人存在时采集兵中断采集、撤回基地且不产出，清场进入倒计时后才恢复行动。
- 采集兵基础移动速度故意设置较慢，移动速度强化每阶 +20%，用于提供清晰的成长反馈。
- 局内粮草和局外玉石使用右上角紧凑的“文字图标 + 数字”显示；不再占用整行资源面板，基地与采集区域相应上移以扩大战场。
- 游戏启动后先进入“长坂行营”局外首页；开始守城进入当前战斗，结算后可以返回行营。
- 点击行营左上角头像进入主将档案页。挑战次数在每次开始守城时累计，最高挑战进度按当前关卡已完成波数占比保存；头像、战役和天赋数量暂以 0 为基础值，随以后功能调整。
- 主将档案使用居中的局部弹窗，打开后保留并压暗行营背景；弹窗仅通过右上角“×”关闭，不显示文字返回按钮。
- 点击档案中的当前头像进入同尺寸头像选择弹窗。首版用 12 个汉字占位头像测试，前 6 个解锁；锁定头像显示灰色蒙版和锁。列表支持触摸上下滑动与鼠标滚轮，选择其他已解锁头像后才能点击黄色“更换头像”，选择结果随存档保存。
- 头像选择弹窗使用左上角返回箭头返回个人信息。头像网格连续铺满可视区域，边缘允许显示不完整头像；手机触摸、鼠标按住拖动和鼠标滚轮均可上下浏览。
- 局外首页的玉石与局内资源共用相同的圆形文字图标加数字样式；首页只保留功能名称，不显示辅助描述或重复的战役入口。
- 当前局内强化属性：基地生命上限、自动回复血量、自动回复速度；采集兵移动速度、采集数量、采集速度、每处农民个数；刀营和弓营分别强化对应兵种伤害、攻击速度、产生速度。后续允许继续增加维度。
- 题材为三国，参考《赵云与阿斗》的文字变形美术，不照搬其合成玩法。
- 初版兵种、各项数值、强化项目为演示默认值，允许用户通过后续表格修改，不能把这些默认值当作已确认需求。

## 3. 开发原则

1. 用户没有编程基础。需要用户操作时，必须使用中文逐步说明按钮位置和点击方法，不假定用户认识技术术语。
2. 用户描述产品思路后，由 Codex 负责方案拆解、代码实现、资源组织和测试；除非缺少会改变产品方向的关键信息，不要求用户手动编写代码。
3. 先完成可运行的最小版本，再逐步加入数值、内容、美术、音效和平台能力。
4. 每次修改后至少确认项目能启动；涉及具体玩法时，还要验证对应操作和状态变化。
5. 优先使用 Godot 原生节点与 GDScript，控制第三方依赖数量，以降低微信小游戏适配难度。
6. 所有界面需要适配不同手机比例，并考虑刘海、圆角和底部安全区域。
7. 按钮和交互区域应适合手指点击，重要信息不能只依赖鼠标悬停显示。
8. 微信小游戏导出方案必须先用最小示例验证成功，再加入登录、广告、排行榜、支付等微信能力。

## 4. 文件与目录规范

随着项目开发，优先使用以下结构：

```text
res://
  scenes/          场景
  scripts/         GDScript 脚本
  assets/
    images/        图片与图集
    fonts/         字体
    audio/         音效与音乐
  data/            数值配置与静态数据
  ui/              可复用界面场景和主题
  addons/          Godot 插件
  tests/           必要的测试或测试场景
```

- 文件夹和代码文件使用小写英文及下划线，例如 `enemy_spawner.gd`。
- 场景按功能命名，例如 `main_game.tscn`、`tower.tscn`、`enemy.tscn`。
- 不使用空格、中文或特殊符号作为新建资源的文件名，避免导出和跨电脑同步问题。
- `.godot/` 是本机缓存，不上传 GitHub。
- Godot 生成的 `.uid` 文件需要和对应脚本一起上传。
- 导出文件统一放入 `builds/`，并默认排除在 Git 之外。
- 不把密码、访问令牌、微信 AppSecret、签名文件或其他账号凭据提交到 GitHub。

## 5. 代码规范

- 脚本、变量、函数使用英文命名；说明和注释可以使用中文。
- 场景负责组合节点，脚本负责行为；避免把整个游戏集中在一个超大脚本中。
- 可调数值集中管理，避免在多个脚本中重复写死。
- 节点之间优先使用信号通信，减少相互强依赖。
- 存档数据必须带版本号，后续调整结构时提供兼容或迁移处理。
- 需要随机结果时，明确随机范围、权重和保底规则，保证数值可以复现和测试。
- 每次新增功能都要处理暂停、重开、退出和重复点击等常见状态。

## 6. 美术与音频规范

- 正式资源必须确认拥有商用授权，并保留来源或授权记录。
- 图片优先使用适合移动端的尺寸并压缩，避免无意义的大图和重复资源。
- 字体需要随项目打包并具有可用于游戏发布的授权，不能长期依赖电脑系统字体。
- 音效与音乐控制体积，并提供独立的音量开关。
- 项目确定正式视觉风格后，再统一颜色、字号、按钮、图标和动画表现。

## 7. GitHub 工作流程

### 公司电脑完成工作后

1. 保存 Godot 场景和资源。
2. 在 GitHub Desktop 查看 `Changes`，确认没有账号密码、缓存或无关文件。
3. 在 `Summary` 填写简短中文说明，例如“创建主界面”。
4. 点击 `Commit to main`。
5. 点击 `Push origin`，确认上传完成。

### 家中电脑开始工作前

第一次使用：

1. 安装相同版本的 Godot，优先使用 4.7.1 stable。
2. 在 GitHub Desktop 登录同一 GitHub 账号。
3. 使用 `Clone repository` 下载仓库。
4. 在 Godot 项目管理器中导入仓库根目录的 `project.godot`。

以后每次开始前：

1. 关闭正在运行的游戏测试窗口。
2. 在 GitHub Desktop 点击 `Fetch origin`。
3. 如果出现 `Pull origin`，点击它下载另一台电脑的最新修改。
4. 再打开 Godot 项目开始工作。

如果两台电脑都修改了同一个文件并出现冲突，不要随意删除文件或强制覆盖，应先让 Codex检查冲突并合并。

## 8. 每次交接应记录的内容

每次结束较大阶段时，更新本文件的“当前进度”，并记录：

- 本次完成了什么。
- 如何运行和验证。
- 还存在什么问题。
- 下一步准备做什么。
- 是否新增了插件、工具或外部资源。

## 9. 新会话给 Codex 的启动指令

在新的 Codex 对话中，可直接发送：

> 请先读取项目根目录的 AGENTS.md 和当前项目文件，检查 Git 状态，然后按照其中的规范继续开发。不要让我手动编写代码；需要我操作软件时，请用中文逐步说明按钮位置。

Codex 开始工作前应先检查 `project.godot`、当前场景、Git 状态和未提交修改，不得覆盖用户在另一台电脑上尚未提交的内容。

## 网页试玩交接
网页导出与固定网址更新方式见 WEB_PLAYTEST.md。Web 导出预设已加入，GitHub 自动发布尚未配置。
